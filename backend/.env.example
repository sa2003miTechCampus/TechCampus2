# Copy to .env and adjust as needed. All variables are optional; sensible
# defaults are already set in app/config.py.

HALAL_CORS_ORIGINS=["*"]
HALAL_QUOTE_CACHE_TTL_SECONDS=300
HALAL_FUNDAMENTALS_CACHE_TTL_SECONDS=3600
HALAL_HISTORY_CACHE_TTL_SECONDS=900
HALAL_DAILY_RECOMMENDATIONS_CACHE_TTL_SECONDS=1800
HALAL_TECHNICAL_WEIGHT=0.6
HALAL_FUNDAMENTAL_WEIGHT=0.4
